<div class="container">
  <div id="sessionForm" class="row">
    <form class="form-horizontal" action="/<%= book.id %>" method="POST">
      <div class="form-group">
      	<label class=" col-sm-offset-2 col-sm-2 control-label" for="title">Title</label>
        <div class="col-sm-4 control-label">
          <input class="form-control" type="text" name="title" placeholder="title" value="<%= book.title %>" required>
        </div>
      </div>
      <div class="form-group">
      	<label class=" col-sm-offset-2 col-sm-2 control-label" for="author">Author</label>
        <div class="col-sm-4 control-label">
      	 <input class="form-control" type="text" name="author" placeholder="author" value="<%= book.author %>" required>
        </div>
      </div>
      <div class="form-group">
      	<label class=" col-sm-offset-2 col-sm-4 control-label" for="description">Summary</label>
      	<textarea class="form-control" rows="5" name="description" placeholder="Summary" required=""><%= book.description %></textarea>
      </div>
      <div class="form-group">
        <label class=" col-sm-offset-2 col-sm-2 control-label" for="url">URL</label>
        <div class="col-sm-4 control-label">
         <input class="form-control" type="text" name="author" placeholder="URL" value="<%= book.imgurl  %>" required>
        </div>
      	<input type="submit" value="ADD BOOK" class="btn btn-primary btn-lg btn btn-primary btn-lg col-sm-4 col-sm-offset-4">
      	<% if(book.id != "") { %>
      	<input type="hidden" name="_method" value="PUT">
      	<% } %>
    </form>
  </div>
</div>